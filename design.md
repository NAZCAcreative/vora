# VARA Design Rules

이 문서는 모든 화면 개발 시 공통으로 따라야 하는 디자인/반응형/동작 규칙이다. 새 화면을 만들거나 기존 화면을 수정할 때 이 기준을 우선 적용한다.

## 공통 구조

- 모든 메인 화면은 공통 상단 네비 `TopNavigation`과 공통 하단 네비 `BottomNavigation`을 사용한다.
- 화면 내부에 별도 상단 네비나 하단 네비를 중복으로 만들지 않는다.
- 기존 페이지에 남아 있는 개별 header/nav는 공통 네비와 충돌하지 않도록 제거하거나 숨김 정책에 맞춘다.
- 학생 모드와 선생님 모드는 라우트 영역을 분리한다.
  - 학생 홈: `/home`
  - 학생 내 정보: `/profile-setup`
  - 선생님 홈: `/homeT`
  - 선생님 내 정보: `/profileT`
- 공통 상단 네비의 로고와 프로필 사진 링크도 현재 모드에 맞춰 이동해야 한다.

## 상단 네비

- 상단 네비는 로고 중앙, 우측에 설정/알림/프로필 순서로 유지한다.
- 로고 클릭:
  - 학생 모드에서는 `/home`
  - 선생님 모드에서는 `/homeT`
- 프로필 사진 클릭:
  - 학생 모드에서는 `/profile-setup`
  - 선생님 모드에서는 `/profileT`
- 알림 아이콘은 페이지 이동이 아니라 레이어 팝업 슬라이드로 연다.
- 새 알림이 있으면 알림 아이콘 오른쪽 위에 작은 빨간 점을 표시한다.

## 하단 네비

- 하단 네비는 하나의 공통 컴포넌트만 사용한다.
- 탭 높이, 아이콘 크기, 활성 상태 크기는 모든 화면에서 동일해야 한다.
- 활성 탭만 `bg-primary-container`와 `text-on-primary-container`를 사용한다.
- 검색 탭을 눌렀는데 예약 탭이 활성화되는 식의 라우트 매칭 오류가 없어야 한다.
- 학생 하단 네비:
  - 홈 `/home`
  - 검색 `/search`
  - 예약 `/my-bookings`
  - 채팅 `/chat`
  - 마이 `/profile-setup`
- 선생님 하단 네비:
  - 홈 `/homeT`
  - 수업관리 `/lessonsT`
  - 등록 `/registerT`
  - 채팅 `/chatT`
  - 마이 `/profileT`

## 레이어/팝업

- 알림은 오른쪽에서 나오는 레이어 슬라이드 패널을 기본으로 한다.
- 레이어는 배경 딤 처리, 닫기 버튼, 바깥 클릭 닫기, Escape 닫기를 지원한다.
- 레이어 내부에서 링크 이동 시 레이어를 닫는다.
- 모바일에서는 전체 폭에 가깝게, 데스크톱에서는 최대 폭을 제한한다.
- 팝업 안의 CTA나 리스트가 하단 네비에 가려지지 않도록 충분한 하단 패딩을 둔다.

## 알림 디자인

- 필수 알림은 슬라이드 카드로 보여준다.
- 필수 알림 종류:
  - 수업 10분 전 알림
  - 예약 승인/변경/취소 알림
  - 채팅 메시지 알림
  - 결제/환불 알림
  - 선생님 정산/출금 알림
- 최근 알림은 리스트로 제공한다.
- 안 읽은 알림은 작은 점과 연한 배경으로 구분한다.
- `모두 읽음`은 실제 UI 상태가 바뀌어야 한다.

## 여백과 가림 방지

- 고정 하단 네비가 있는 화면은 콘텐츠와 CTA가 네비에 가려지지 않아야 한다.
- 하단 고정 CTA는 보통 `bottom-20` 이상에 배치한다.
- 메인 콘텐츠에는 화면 성격에 따라 `pb-28`, `pb-32`, `pb-48`, `pb-56` 등 충분한 하단 여백을 둔다.
- 결제, 예약, 프로필 보기 등 주요 CTA가 하단 네비 아래로 숨으면 안 된다.
- 불필요한 큰 빈 공간, 과한 상단 보정값, 중복 패딩을 피한다.
- 공통 상단 네비가 있으므로 개별 페이지에서 `pt-16`, `pt-20`, `pt-24` 같은 보정값을 무조건 추가하지 않는다.

## 반응형

- 모바일 우선으로 구성하고, 데스크톱에서는 `max-w-*`와 그리드로 확장한다.
- 폼/설정/정산/출금 화면은 데스크톱에서 2컬럼 구성을 우선 고려한다.
- 작은 폼 화면을 무리하게 데스크톱 전체 폭으로 늘리지 않는다.
- 버튼 텍스트가 줄바꿈 없이 잘리거나 부모 영역을 넘어가면 안 된다.
- 카드 내부 텍스트와 버튼은 모바일에서도 겹치지 않아야 한다.
- 고정 포맷 UI는 안정적인 높이/폭을 둬서 hover, 선택, 로딩 상태에서 레이아웃이 흔들리지 않게 한다.

## 버튼과 동작

- 의미 없는 버튼은 만들지 않는다.
- 클릭 동작이 없는 버튼은 다음 중 하나로 처리한다.
  - 실제 라우트 연결
  - 상태 변경 구현
  - 준비 중/비활성 상태 명시
  - 제거
- `href="#"`는 사용하지 않는다.
- 저장/등록/신청/결제 계열 버튼은 성공 상태, 다음 화면 이동, 또는 명확한 준비 중 상태가 있어야 한다.
- 토글은 ON/OFF 상태가 시각적으로 명확해야 한다.
  - ON: `bg-primary`
  - OFF: `bg-surface-container-highest`
  - knob 위치가 정확히 이동해야 한다.
- 아이콘 버튼에는 접근 가능한 `aria-label`을 둔다.

## 카드와 레이아웃

- 카드는 반복 항목, 모달, 실제 프레임이 필요한 도구에만 사용한다.
- 페이지 전체 섹션을 카드 안에 또 카드로 감싸지 않는다.
- 카드 radius는 기본적으로 `rounded-xl` 이하를 사용한다.
- 정보 카드에는 얇은 border와 부드러운 shadow를 사용한다.
- 가격, 총 결제 금액 같은 요약 영역은 사용자가 요청하지 않은 화면에서는 불필요하게 반복하지 않는다.
- 프로필 보기 화면에서는 하단 CTA가 핵심이며, 가격 정보는 노출하지 않는다.

## 색상과 타이포그래피

- 기존 Tailwind theme token을 우선 사용한다.
- 주요 액션은 `primary`, 보조 강조는 `secondary`, 정보성 강조는 `tertiary`를 사용한다.
- 한 화면이 한 가지 색조만으로 보이지 않게 한다.
- 큰 hero용 텍스트를 카드/폼 내부에 남용하지 않는다.
- 버튼, 탭, 카드 내부 글자는 컨테이너 크기에 맞는 작은 heading/body scale을 사용한다.
- letter spacing을 과하게 줄이지 않는다.

## 학생/선생님 데이터 일관성

- 같은 선생님이 예약, 결제, 채팅, 알림, 상세 페이지에서 다른 이름으로 보이면 안 된다.
- 학생 모드의 내 정보와 선생님 모드의 내 정보는 서로 섞이면 안 된다.
- 선생님 모드에서 학생 모드로 전환하는 버튼은 명확히 전환 목적을 보여준다.

## 화면 검수 체크리스트

- [ ] 공통 상단 네비가 보이는가?
- [ ] 공통 하단 네비가 보이는가?
- [ ] 현재 라우트에 맞는 하단 탭이 활성화되는가?
- [ ] 학생/선생님 모드 링크가 섞이지 않았는가?
- [ ] 하단 네비가 CTA나 입력창을 가리지 않는가?
- [ ] 모바일에서 버튼 텍스트가 깨지거나 겹치지 않는가?
- [ ] 데스크톱에서 화면이 지나치게 좁거나 넓지 않은가?
- [ ] 의미 없는 버튼, `href="#"`, 클릭 액션 없는 아이콘이 남아 있지 않은가?
- [ ] 토글, 탭, 필터 등 상태형 UI가 실제로 상태를 바꾸는가?
- [ ] 알림/예약/채팅/결제 같은 핵심 흐름은 관련 화면으로 이동하는가?

## 개발 전 확인

- 새 화면을 만들기 전에 기존 공통 컴포넌트를 먼저 확인한다.
- 새 네비, 새 토글, 새 버튼 스타일을 만들기 전에 기존 패턴을 재사용한다.
- 화면 완료 후 최소한 주요 라우트 응답과 `tsc --noEmit`을 확인한다.
- 동작하지 않는 버튼은 `check.md`에 남기고, 가능하면 같은 작업 안에서 실제 동작으로 연결한다.
